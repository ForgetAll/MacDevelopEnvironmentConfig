[credential]
    helper = store
[core]
    editor = vim
    pager = diff-so-fancy | less --tabs=4 -RFX
    safecrlf = true
    autocrlf = false
[alias]
    co = checkout
    s = status
    d = diff
    b = branch
    l = log --graph --oneline
